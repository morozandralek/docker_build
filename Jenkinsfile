pipeline {
    agent any
    stages {
        stage('Test curl') {
            agent {
                label 'node2'
            }
            steps(create mkdir) {
                sh """
                 
                """
            }
        }
    }
}
